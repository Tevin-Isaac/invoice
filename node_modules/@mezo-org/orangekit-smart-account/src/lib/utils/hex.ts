import { ethers } from "ethers"

// TODO: We should probably use our own Hex as in tbtc-v2.ts lib
export type Hex = `0x${string}`

type ToHexData = string

export function toHex(data: ToHexData) {
  return ethers.hexlify(data) as Hex
}
