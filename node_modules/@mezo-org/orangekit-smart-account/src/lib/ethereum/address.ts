import { ethers } from "ethers"
import { Hex } from "../utils"

// eslint-disable-next-line import/prefer-default-export
export function getAddress(address: string) {
  return ethers.getAddress(address) as Hex
}
