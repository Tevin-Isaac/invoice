import { JsonRpcProvider, Wallet } from "ethers";
export default class LocalTransactionSender {
    #provider;
    #signer;
    constructor(relayerPrivateKey) {
        this.#provider = new JsonRpcProvider("http://localhost:8545");
        this.#signer = new Wallet(relayerPrivateKey, this.#provider);
    }
    async sendTransaction(tx) {
        return this.#signer.sendTransaction(tx);
    }
}
//# sourceMappingURL=LocalTransactionSender.js.map