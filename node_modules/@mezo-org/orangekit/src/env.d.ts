import { InjectedProvider } from "./wallet/bitcoin-wallet-provider"

declare global {
  interface Window {
    unisat?: InjectedProvider
    okxwallet?: { bitcoin: InjectedProvider }
    XverseProviders?: { BitcoinProvider: InjectedProvider }
  }
}
