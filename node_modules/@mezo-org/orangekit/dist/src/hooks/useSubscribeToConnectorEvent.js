import { useEffect } from "react";
import { useConnectors } from "wagmi";
export function useSubscribeToConnectorEvent(config, connectorId, eventName, callback) {
    const connectors = useConnectors({ config });
    const connector = connectors.find((c) => c.id === connectorId);
    useEffect(() => {
        if (!connector) {
            return undefined;
        }
        connector.emitter.on(eventName, callback);
        return () => {
            connector.emitter.off(eventName, callback);
        };
    }, [connector, callback, connectorId, eventName]);
}
//# sourceMappingURL=useSubscribeToConnectorEvent.js.map